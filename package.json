{
  "name": "link-shortener",
  "version": "1.0.0",
  "description": "Team Link Shortener",
  "scripts": {
    "build": "echo 'Build complete'",
    "postinstall": "cd functions && if [ -f requirements.txt ]; then pip install -r requirements.txt -t python; fi"
  },
  "engines": {
    "node": ">=18"
  }
}
